# Web app
fastapi
uvicorn[standard]

# LLM (chat) — Groq's free tier phrases the final reply. It does not
# choose the product; recommendation_engine.py does that deterministically
# with no ML model involved.
langchain-core
langchain-groq

# NOTE: the live app's symptom matching (recommendation_engine.py,
# intent_parser.py) is pure Python — no embeddings, no vector DB, no extra
# dependency. That keeps this safe to run in a small/constrained container.

# Everything below is ONLY used by the optional offline RAG CLI scripts
# (create_memory_for_llm.py / connect_memory_with_llm.py) and is not
# imported by the live web app (app.py) at all.
langchain-community
langchain-huggingface
sentence-transformers
langchain-text-splitters
langchain-classic
faiss-cpu
pypdf
